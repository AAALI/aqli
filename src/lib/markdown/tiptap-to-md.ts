/**
 * Kept as the import path used across the app. The hand-rolled string
 * manipulation this file used to hold has been replaced by an explicit
 * `prosemirror-markdown` serializer — see `./serializer.ts`.
 */
export { tiptapToMarkdown } from "./index";

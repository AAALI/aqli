/**
 * Kept as the import path used across the app. The hand-rolled line scanner
 * this file used to hold has been replaced by an explicit
 * `prosemirror-markdown` parser — see `./parser.ts`.
 */
export { markdownToTiptap } from "./index";

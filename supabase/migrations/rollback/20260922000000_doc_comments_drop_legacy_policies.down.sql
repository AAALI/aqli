-- Deliberately empty: the dropped policies let any member forge review-trail
-- entries. Restoring them would reintroduce that; the replacements from
-- 20260808000000 cover the same legitimate access.
select 1;
